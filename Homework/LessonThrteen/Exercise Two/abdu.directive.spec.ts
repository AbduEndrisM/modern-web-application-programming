import { AbduDirective } from './abdu.directive';

describe('AbduDirective', () => {
  it('should create an instance', () => {
    const directive = new AbduDirective();
    expect(directive).toBeTruthy();
  });
});
